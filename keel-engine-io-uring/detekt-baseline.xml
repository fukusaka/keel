<?xml version="1.0" encoding="UTF-8"?>
<SmellBaseline>
  <ManuallySuppressedIssues></ManuallySuppressedIssues>
  <CurrentIssues>
    <ID>ArgumentListWrapping:IoUringEventLoop.kt$IoUringEventLoop$( threadPtr.ptr, null, staticCFunction { arg -> val el = arg!!.asStableRef&lt;IoUringEventLoop>().get() el.loop() arg.asStableRef&lt;IoUringEventLoop>().dispose() null }, ref.asCPointer(), )</ID>
    <ID>ArgumentListWrapping:IoUringEventLoopGroup.kt$IoUringEventLoopGroup$(loops[i], logger, bufferCount = bufferRingSlotCount, bufferSize = readBufferSize, bgid = i)</ID>
    <ID>ArgumentListWrapping:IoUringIoTransport.kt$IoUringIoTransport$( sqeFd, ptr, length.convert(), MSG_NOSIGNAL, bufIndex = bufIndex, fixedFile = useFixedFile, )</ID>
    <ID>ArgumentListWrapping:IoUringIoTransport.kt$IoUringIoTransport$(sqeFd, ptr, length.convert(), MSG_NOSIGNAL, fixedFile = useFixedFile)</ID>
    <ID>ComplexCondition:IoUringIoTransport.kt$IoUringIoTransport$opened &amp;&amp; readEnabled &amp;&amp; !recvStarved &amp;&amp; recvSlot &lt; 0</ID>
    <ID>CyclomaticComplexMethod:IoUringEventLoop.kt$IoUringEventLoop$internal fun runIteration(cqe: Cqe): Boolean</ID>
    <ID>ImportOrdering:IoUringEventLoop.kt$import io_uring.io_uring import io_uring.io_uring_prep_cancel64 import io_uring.io_uring_prep_read import io_uring.io_uring_prep_accept import io_uring.io_uring_prep_writev import io_uring.iovec import io_uring.io_uring_sqe import io_uring.io_uring_sqe_set_data64 import io_uring.keel_prep_msg_ring import io_uring.keel_prep_recv import io_uring.keel_prep_recv_buf_select import io_uring.keel_prep_recv_multishot import io_uring.keel_prep_send_zc import io_uring.keel_prep_sendmsg_zc import io_uring.keel_prep_send_zc_fixed import io_uring.keel_register_iowq_max_workers import io_uring.keel_register_napi import io_uring.keel_register_ring_fd import io_uring.keel_ring_fd import io_uring.keel_sqe_set_fixed_file import io_uring.keel_unregister_napi import io_uring.keel_feat_nodrop import io_uring.keel_unregister_ring_fd import kotlinx.cinterop.Arena import kotlinx.cinterop.ByteVar import kotlinx.cinterop.COpaquePointer import kotlinx.cinterop.CPointer import kotlinx.cinterop.ExperimentalForeignApi import kotlinx.cinterop.StableRef import kotlinx.cinterop.ULongVar import kotlinx.cinterop.alloc import kotlinx.cinterop.memScoped import kotlinx.cinterop.UIntVar import kotlinx.cinterop.asStableRef import kotlinx.cinterop.get import kotlinx.cinterop.pointed import kotlinx.cinterop.ptr import kotlinx.cinterop.staticCFunction import kotlinx.cinterop.value import io.github.fukusaka.keel.buf.MpscQueue import io.github.fukusaka.keel.logging.Logger import io.github.fukusaka.keel.logging.debug import io.github.fukusaka.keel.logging.error import io.github.fukusaka.keel.logging.warn import io.github.fukusaka.keel.native.posix.closeFdSafely import io.github.fukusaka.keel.native.posix.errnoMessage import io.github.fukusaka.keel.pipeline.DeadlineScheduler import kotlinx.coroutines.CancellableContinuation import kotlinx.coroutines.CoroutineDispatcher import kotlinx.coroutines.Runnable import kotlinx.coroutines.suspendCancellableCoroutine import kotlin.time.TimeSource import platform.posix.EAGAIN import platform.posix.EINTR import platform.posix.ETIME import platform.posix.errno import platform.posix.pthread_create import platform.posix.pthread_equal import platform.posix.pthread_join import platform.posix.pthread_self import platform.posix.pthread_t import platform.posix.pthread_tVar import kotlin.concurrent.AtomicInt import kotlin.concurrent.AtomicLong import kotlin.coroutines.CoroutineContext import kotlin.coroutines.EmptyCoroutineContext import kotlin.coroutines.resume</ID>
    <ID>ImportOrdering:IoUringIoTransport.kt$import io.github.fukusaka.keel.buf.BufferAllocator import io_uring.keel_prep_shutdown import io_uring.keel_sqe_set_fixed_file import io.github.fukusaka.keel.buf.IoBuf import io.github.fukusaka.keel.buf.UnsafeIoBufApi import io.github.fukusaka.keel.buf.unsafePointer import io.github.fukusaka.keel.logging.debug import io.github.fukusaka.keel.logging.warn import io.github.fukusaka.keel.native.posix.NativeSocket import io.github.fukusaka.keel.native.posix.PosixNativeSocket import io.github.fukusaka.keel.native.posix.ShutdownResult import io.github.fukusaka.keel.native.posix.WriteResult import io.github.fukusaka.keel.native.posix.closeFdSafely import io.github.fukusaka.keel.native.posix.errnoMessage import io.github.fukusaka.keel.pipeline.AbstractIoTransport import io.github.fukusaka.keel.pipeline.AbstractIoTransport.PendingWrite import io.github.fukusaka.keel.pipeline.EventLoopTimer import io.github.fukusaka.keel.pipeline.IoTransport import io.github.fukusaka.keel.pipeline.PendingWriteSnapshotPool import kotlinx.coroutines.CoroutineDispatcher import kotlinx.coroutines.Runnable import kotlinx.coroutines.suspendCancellableCoroutine import kotlin.coroutines.EmptyCoroutineContext import kotlin.coroutines.resume import io_uring.KEEL_POLLERR import io_uring.KEEL_POLLHUP import io_uring.KEEL_POLLRDHUP import io_uring.io_uring_prep_send import io_uring.keel_cqe_get_buf_id import io_uring.keel_cqe_has_more import io_uring.keel_prep_poll_add import platform.posix.ECANCELED import platform.posix.ENOBUFS import platform.posix.SHUT_WR import io_uring.iovec import io_uring.keel_alloc_iovec import io_uring.keel_free_iovec import kotlinx.cinterop.ByteVar import kotlinx.cinterop.COpaquePointerVar import kotlinx.cinterop.CPointerVar import kotlinx.cinterop.ExperimentalForeignApi import kotlinx.cinterop.ULongVar import kotlinx.cinterop.allocArray import kotlinx.cinterop.convert import kotlinx.cinterop.memScoped import kotlinx.cinterop.plus import kotlinx.cinterop.reinterpret import kotlinx.cinterop.set import platform.posix.MSG_NOSIGNAL</ID>
    <ID>ImportOrdering:PosixIoUringRing.kt$import io_uring.io_uring import io_uring.io_uring_cqe_get_data64 import io_uring.io_uring_cqe_seen import io_uring.io_uring_get_sqe import io_uring.io_uring_queue_exit import io_uring.keel_queue_init_params import io_uring.io_uring_sqe import io_uring.io_uring_submit import io_uring.io_uring_submit_and_wait import io_uring.keel_cqe_has_more import io_uring.keel_submit_and_wait_timeout import io_uring.keel_peek_cqe import io_uring.keel_setup_coop_taskrun import io_uring.keel_setup_defer_taskrun import io_uring.keel_setup_single_issuer import kotlinx.cinterop.CPointer import kotlinx.cinterop.ExperimentalForeignApi import kotlinx.cinterop.UIntVar import kotlinx.cinterop.pointed</ID>
    <ID>LargeClass:IoUringIoTransport.kt$IoUringIoTransport : AbstractIoTransport</ID>
    <ID>LongParameterList:IoUringEngine.kt$IoUringEngine$( override val config: IoEngineConfig = IoEngineConfig(), private val writeModeSelector: IoModeSelector = IoModeSelectors.eagainThreshold(), capabilities: IoUringCapabilities? = null, private val nativeSocket: NativeSocket = PosixNativeSocket, nativeSocketOps: NativeSocketOps? = null, registeredBufferStrategy: RegisteredBufferStrategy = RegisteredBufferStrategy.STATIC, registeredBufferSlotCount: Int = IoUringEventLoopGroup.DEFAULT_REGISTERED_BUFFER_SLOT_COUNT, registeredBufferSize: Int = IoTransport.DEFAULT_READ_BUFFER_SIZE, bufferRingSlotCount: Int = ProvidedBufferRing.DEFAULT_BUFFER_COUNT, /** * SQE (submission) ring size per EventLoop (power of 2). Defaults to * [IoUringEventLoop.DEFAULT_RING_SIZE]. Raise it for very high fan-out * workloads that prepare many SQEs per loop tick. */ ringSize: Int = IoUringEventLoop.DEFAULT_RING_SIZE, /** * CQE (completion) ring size per EventLoop. `0` (default) leaves the kernel * default of `2 * ringSize`; a positive power-of-2 `>= ringSize` sets * `IORING_SETUP_CQSIZE` so the CQ ring absorbs more completions before * overflow (useful for multishot-heavy or high-completion-rate workloads). */ cqSize: Int = 0, )</ID>
    <ID>LongParameterList:IoUringEventLoopGroup.kt$IoUringEventLoopGroup$( size: Int, private val logger: Logger, allocator: BufferAllocator, capabilities: IoUringCapabilities = IoUringCapabilities(), ringSize: Int = IoUringEventLoop.DEFAULT_RING_SIZE, cqSize: Int = 0, val readBufferSize: Int = IoTransport.DEFAULT_READ_BUFFER_SIZE, idleTimeoutMillis: Long = 0, /** * Per-EventLoop Fixed Buffer registry strategy. Resolved against * `capabilities.registeredBuffers`: `STATIC` falls back to `DISABLED` * with a warn log when the kernel lacks `IORING_REGISTER_BUFFERS`; * `DYNAMIC` (not yet implemented) is rejected with an early * `IllegalStateException`. See [RegisteredBufferStrategy] for the * per-value semantics. */ private val registeredBufferStrategy: RegisteredBufferStrategy = RegisteredBufferStrategy.STATIC, /** * Per-EventLoop upper bound on the number of buffers the STATIC warmup * touches before enumeration. The registered set is whatever the * allocator's pool holds after warmup, so the effective count is * clamped by the pool's own slot capacity — values above it register * the pool capacity, not more. Consulted by STATIC only. */ private val registeredBufferSlotCount: Int = DEFAULT_REGISTERED_BUFFER_SLOT_COUNT, /** * Size in bytes of each buffer the STATIC warmup allocates. Should * match the allocator's pooled size class (the read-buffer class by * default) — other sizes miss the pool and contribute nothing to the * registered set. Consulted by STATIC only. */ private val registeredBufferSize: Int = IoTransport.DEFAULT_READ_BUFFER_SIZE, /** * Number of recv buffers in each EventLoop's [ProvidedBufferRing] * (kernel requirement: a power of two — validated at ring construction). * Shared by every connection on the loop; bounds in-flight deliveries * before copy-on-pressure kicks in. */ bufferRingSlotCount: Int = ProvidedBufferRing.DEFAULT_BUFFER_COUNT, )</ID>
    <ID>LongParameterList:IoUringIoTransport.kt$IoUringIoTransport$( private val fd: Int, private val eventLoop: IoUringEventLoop, private val capabilities: IoUringCapabilities, private val writeModeSelector: IoModeSelector = IoModeSelectors.FALLBACK_CQE, allocator: BufferAllocator, private val bufferRing: ProvidedBufferRing? = null, private val fixedFileRegistry: FixedFileRegistry? = null, private val registeredBufferTable: IoUringFixedBufferRegistry = DisabledRegisteredBufferRegistry, /** * Kernel-allocated fixed-file index for direct-allocated multishot accept. * When >= 0, the kernel has already placed the fd into the registered * file table at this index (see [IoUringCapabilities.acceptDirectAlloc]); * the raw [fd] is not exposed to userspace and should be passed as -1. * When -1 (default), [fd] is the raw POSIX fd and is registered via * [FixedFileRegistry.register]. */ preAllocatedIndex: Int = -1, private val nativeSocket: NativeSocket = PosixNativeSocket, idleTimeoutMillis: Long = 0, /** * Per-recv allocation size of the allocator-buffer read fallback * ([armRecvSingleShotAlloc], kernels without a provided buffer ring). * Ring-capable kernels ignore it — their recv buffer size is the * ring's own `bufferSize`. Defaults to the engine-wide read buffer * size via [IoUringEventLoopGroup.readBufferSize] at the call sites. */ private val readBufferSize: Int = IoTransport.DEFAULT_READ_BUFFER_SIZE, )</ID>
    <ID>LongParameterList:IoUringStreamServer.kt$IoUringStreamServer$( private val serverFd: Int, private val bossLoop: IoUringEventLoop, private val workerGroup: IoUringEventLoopGroup, override val localAddress: SocketAddress, private val bindConfig: BindConfig, private val writeModeSelector: IoModeSelector = IoModeSelectors.FALLBACK_CQE, private val capabilities: IoUringCapabilities = IoUringCapabilities(), private val logger: Logger = io.github.fukusaka.keel.logging.NoopLoggerFactory.logger("IoUringStreamServer"), private val nativeSocket: NativeSocket = PosixNativeSocket, private val nativeSocketOps: NativeSocketOps = PosixNativeSocketOps(logger), )</ID>
    <ID>MaxLineLength:IoUringEngine.kt$IoUringEngine$serverFd</ID>
    <ID>MaximumLineLength:IoUringEngine.kt$IoUringEngine$ </ID>
    <ID>MultiLineIfElse:IoModeSelector.kt$IoModeSelectors$IoMode.CQE</ID>
    <ID>MultiLineIfElse:IoModeSelector.kt$IoModeSelectors$IoMode.FALLBACK_CQE</ID>
    <ID>MultiLineIfElse:IoUringEngine.kt$IoUringEngine.Companion$config.threads</ID>
    <ID>MultiLineIfElse:IoUringEngine.kt$IoUringEngine.Companion$kotlin.native.Platform.getAvailableProcessors()</ID>
    <ID>MultiLineIfElse:IoUringIoTransport.kt$IoUringIoTransport$eventLoop.dispatch(EmptyCoroutineContext, register)</ID>
    <ID>MultiLineIfElse:IoUringIoTransport.kt$IoUringIoTransport$register.run()</ID>
    <ID>NoBlankLineBeforeRbrace:IoUringIoTransport.kt$IoUringIoTransport$ </ID>
    <ID>NoBlankLineBeforeRbrace:IoUringStreamServer.kt$IoUringStreamServer$ </ID>
    <ID>NoUnusedImports:IoUringEventLoop.kt$io.github.fukusaka.keel.engine.iouring.IoUringEventLoop.kt</ID>
    <ID>NoUnusedImports:IoUringStreamServer.kt$io.github.fukusaka.keel.engine.iouring.IoUringStreamServer.kt</ID>
    <ID>ParameterListWrapping:IoUringEventLoop.kt$IoUringEventLoop$( fd: Int, buf: COpaquePointer, len: ULong, flags: Int, bufIndex: Int, fixedFile: Boolean = false, onComplete: (bytesOrError: Int) -> Unit, )</ID>
    <ID>ParameterListWrapping:IoUringEventLoop.kt$IoUringEventLoop$( fd: Int, buf: COpaquePointer, len: ULong, flags: Int, fixedFile: Boolean = false, onComplete: (bytesOrError: Int) -> Unit, )</ID>
    <ID>ParameterListWrapping:IoUringEventLoop.kt$IoUringEventLoop$( fd: Int, iovecs: CPointer&lt;iovec>, count: UInt, fixedFile: Boolean = false, onComplete: (bytesOrError: Int) -> Unit, )</ID>
    <ID>ParameterListWrapping:IoUringIoTransport.kt$IoUringIoTransport$( buf: IoBuf, offset: Int, length: Int, onComplete: () -> Unit, )</ID>
    <ID>SpacingBetweenDeclarationsWithAnnotations:RingBufferIoBuf.kt$RingBufferIoBuf$@UnsafeIoBufApi override val unsafePointer: CPointer&lt;ByteVar> get() = ptr</ID>
    <ID>SpacingBetweenDeclarationsWithComments:IoUringEngine.kt$IoUringEngine$// Internal for the in-tree occupancy measure (linuxTest), which reads</ID>
    <ID>SpacingBetweenDeclarationsWithComments:IoUringEventLoop.kt$IoUringEventLoop$// Callback slots shared by submitMultishot (multishot SQEs) and</ID>
    <ID>SpacingBetweenDeclarationsWithComments:IoUringEventLoop.kt$IoUringEventLoop$// SEND_ZC fire-and-forget: callback invoked with send result after both CQEs arrive.</ID>
    <ID>SpacingBetweenDeclarationsWithComments:IoUringEventLoop.kt$IoUringEventLoop$// SEND_ZC: stores the first CQE result while waiting for the second (notification) CQE.</ID>
    <ID>SpacingBetweenDeclarationsWithComments:IoUringEventLoopGroup.kt$IoUringEventLoopGroup$// Whether the kernel supports registered buffers (SEND_ZC_FIXED). Captured as a</ID>
    <ID>Wrapping:IoUringEventLoop.kt$IoUringEventLoop$(</ID>
    <ID>Wrapping:IoUringEventLoop.kt$IoUringEventLoop$(EmptyCoroutineContext, Runnable { // If cancelled before this Runnable ran, skip SQE submission: // the caller will receive CancellationException without any in-flight SQE. if (!cont.isActive) return@Runnable val sqe = acquireSqe() prepare(sqe) val slot = acquireSlot() contSlots[slot] = cont val userData = slot.toULong() + SLOT_BASE io_uring_sqe_set_data64(sqe, userData) submittedUserData.value = userData.toLong() })</ID>
    <ID>Wrapping:IoUringEventLoop.kt$IoUringEventLoop$(EmptyCoroutineContext, Runnable { val sqe = ioUringRing.getSqe(ring.ptr) ?: return@Runnable io_uring_prep_cancel64(sqe, ud.toULong(), 0) io_uring_sqe_set_data64(sqe, CANCEL_TOKEN) })</ID>
    <ID>Wrapping:IoUringEventLoop.kt$IoUringEventLoop$(EmptyCoroutineContext, Runnable { val sqe = ioUringRing.getSqe(ring.ptr) ?: return@Runnable io_uring_prep_cancel64(sqe, userData, 0) io_uring_sqe_set_data64(sqe, CANCEL_TOKEN) })</ID>
    <ID>Wrapping:IoUringEventLoopGroup.kt$IoUringEventLoopGroup$(</ID>
    <ID>Wrapping:IoUringEventLoopGroup.kt$IoUringEventLoopGroup$(EmptyCoroutineContext, Runnable { // Warm the per-EventLoop pool and build its registered-buffer table // on the owning pthread, before the kernel registration below. Runs // here (not in the constructor) so the enumerated pool addresses are // resident on the thread that owns them. Behaviour-neutral under // instance-handout (process-global PooledAllocator pools). if (registeredBuffersEnabled) { val alloc = allocators[i] warmupPool(alloc) val pooledAlloc = alloc as? io.github.fukusaka.keel.buf.PooledAllocator val pooled = pooledAlloc?.let { io.github.fukusaka.keel.buf.enumerateNativePooledBuffers(it) } if (pooled != null &amp;&amp; pooled.isNotEmpty()) { bufferTables[i] = StaticRegisteredBufferRegistry(loops[i], pooled, logger) } } // Every non-STATIC outcome (strategy DISABLED, kernel-capability // fallback, non-pooled allocator, empty pool enumeration) gets the // null-object registry, so bufferTableAt never hands out null once // the group has started and downstream plumbing can drop its // nullable handling as it migrates to the interface. if (bufferTables[i] == null) { bufferTables[i] = DisabledRegisteredBufferRegistry } bufferRings[i]?.initOnEventLoop() fileRegistries[i]?.initOnEventLoop() bufferTables[i]?.initOnEventLoop() val lockRet = pthread_mutex_lock(mutex.ptr) if (lockRet != 0) { logger.warn { "pthread_mutex_lock() failed: ${errnoMessage(lockRet)}" } } val remaining = pending.decrementAndGet() if (remaining == 0) { val signalRet = pthread_cond_signal(cond.ptr) if (signalRet != 0) { logger.warn { "pthread_cond_signal() failed: ${errnoMessage(signalRet)}" } } } val unlockRet = pthread_mutex_unlock(mutex.ptr) if (unlockRet != 0) { logger.warn { "pthread_mutex_unlock() failed: ${errnoMessage(unlockRet)}" } } })</ID>
    <ID>Wrapping:IoUringIoTransport.kt$IoUringIoTransport$(</ID>
    <ID>Wrapping:IoUringIoTransport.kt$IoUringIoTransport$(EmptyCoroutineContext, Runnable { teardownOnEventLoop() })</ID>
    <ID>Wrapping:IoUringIoTransport.kt$IoUringIoTransport$;</ID>
    <ID>Wrapping:IoUringStreamServer.kt$IoUringStreamServer$(</ID>
    <ID>Wrapping:IoUringStreamServer.kt$IoUringStreamServer$(EmptyCoroutineContext, Runnable { closeFdSafely(serverFd, logger, "server close") if (multishotSlot != -1) { bossLoop.cancelSqe(multishotSlot) multishotSlot = -1 } // Resume every queued accept waiter with cancellation. while (pendingAcceptConts.isNotEmpty()) { pendingAcceptConts.removeFirst() .resumeWithException(CancellationException("StreamServer closed")) } // Close any queued fds that haven't been accepted yet. while (pendingFds.isNotEmpty()) { closeFdSafely(pendingFds.removeFirst(), logger, "server close (pending fd)") } })</ID>
  </CurrentIssues>
</SmellBaseline>

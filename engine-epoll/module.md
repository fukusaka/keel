# Module engine-epoll

Linux epoll-based IoEngine implementation with multi-threaded EventLoop.

# Package io.github.fukusaka.keel.engine.epoll

EpollEngine, EpollChannel, EpollEventLoop, and pipeline support (EpollPipelinedChannel, EpollIoTransport).

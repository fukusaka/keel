# Module keel-codec-http

HTTP/1.1 codec — parser, writer, and pipeline handlers (RFC 7230/7231).

# Package io.github.fukusaka.keel.codec.http

HttpRequest, HttpResponse, HttpHeaders, HttpParser/HttpWriter for blocking and suspend I/O, and pipeline handlers (HttpRequestDecoder, HttpResponseEncoder, RoutingHandler).

# Module logging

Lightweight logging facade with zero external dependencies.

# Package io.github.fukusaka.keel.logging

Logger, LoggerFactory, LogLevel, and default implementations (NoopLoggerFactory, PrintLogger).

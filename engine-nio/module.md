# Module engine-nio

JVM NIO Selector-based IoEngine implementation with multi-threaded EventLoop.

# Package io.github.fukusaka.keel.engine.nio

NioEngine, NioChannel, NioEventLoop, and pipeline support (NioPipelinedChannel, NioIoTransport). Uses SelectionKey caching for zero-JNI interest ops toggling.
